parameters:
  - name: name
    type: string
    default: DelayJob

jobs:
  - job: ${{ parameters.name}}_stable
    pool: server
    steps:
      - task: Delay@1
        inputs:
          delayForMinutes: '0'